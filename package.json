{
  "name": "alchemist4real",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt && python3 scripts/sync-research.py"
  }
}
